<UserControl x:Class="FocLauncher.Update.Views.UpdateInfoBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300">
    <Border Margin="0,0,0,12" BorderThickness="1" BorderBrush="DodgerBlue">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="DodgerBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Border x:Name="ProgressIndicator" Margin="12,0,0,0" Height="16" Width="16" HorizontalAlignment="Left" Background="Red"/>
                <TextBlock Text="Status" Margin="12,8,8,8" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" TextTrimming="None"/>
            </StackPanel>
            <Button Grid.Column="1" Width="32" Height="32"
                    Visibility="{Binding ShouldShowSearchButton,  Converter={StaticResource BooleanToVisibilityConverter}}"
                    DataContext="{Binding SearchForUpdateCommand}"/>
        </Grid>
    </Border>
</UserControl>
