using System.Threading;

namespace FocLauncherHost.Updater.Tasks
{
    internal sealed class KeepTask : UpdateTask
    {
        protected override void ExecuteTask(CancellationToken token)
        {
        }
    }
}